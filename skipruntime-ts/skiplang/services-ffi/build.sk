fun main(): void {
  srcs = Array["cc/context.cpp", "cc/ffi.c"];
  srcs.each(src -> print_string(`skargo:rerun-if-changed=${src}`));
  Cc.Build().flags(Cc.kRecommendedCflags).files(srcs).compile("services-ffi")
}
